<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <artifactId>modig-frontend</artifactId>
    <packaging>pom</packaging>
    <version>0.5.2-SNAPSHOT</version>

    <parent>
        <groupId>no.nav.modig</groupId>
        <artifactId>modig-main</artifactId>
        <version>1.0.0-alpha.1</version>
    </parent>

    <name>${project.artifactId}</name>
    <description>
        Dette biblioteket inneholder statiske ressurser for felles utseende og oppførsel i moderniserte applikasjoner i Nav.
    </description>
    
    <properties>
    	<modig-wicket.version>0.4.0</modig-wicket.version>
    	<modig-wicket-test.version>0.4.0</modig-wicket-test.version>
    </properties>

    <modules>
        <module>modig-frontend-ressurser</module>
        <module>modig-frontend-wicket</module>
        <module>modig-frontend-testapp</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>no.nav.modig</groupId>
                <artifactId>modig-wicket</artifactId>
                <version>${modig-wicket.version}</version>
            </dependency>
            <dependency>
                <groupId>no.nav.modig</groupId>
                <artifactId>modig-wicket-test</artifactId>
                <version>${modig-wicket-test.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <distributionManagement>
        <repository>
            <id>m2internal</id>
            <name>NAV Maven2 Internal Repository</name>
            <url>http://maven.adeo.no/nexus/content/repositories/m2internal</url>
        </repository>
        <snapshotRepository>
            <id>m2snapshot</id>
            <name>NAV Maven2 Snapshot Repository</name>
            <url>http://maven.adeo.no/nexus/content/repositories/m2snapshot</url>
            <uniqueVersion>false</uniqueVersion>
        </snapshotRepository>
    </distributionManagement>

    <scm>
        <developerConnection>scm:svn:https://versjonskontroll.adeo.no/svn/applikasjonsrammeverk/modig-frontend/trunk
        </developerConnection>
        <url>https://versjonskontroll.adeo.no/svn/applikasjonsrammeverk/modig-frontend/trunk</url>
    </scm>

</project>
